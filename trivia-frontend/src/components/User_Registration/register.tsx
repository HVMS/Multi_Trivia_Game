import React from 'react';
import {Auth} from '../auth'
import './register.css'

const Register = () => {
    return (
        <div>
            <Auth />
        </div>
    );
}

export default Register;